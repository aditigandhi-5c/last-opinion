import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.echomed.secondopinion',
  appName: 'second-opinion-nexus',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;