version: '3.8'

services:
  # Frontend React App
  frontend:
    build: .
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - backend

  # Backend FastAPI App
  backend:
    build: ./backend
    ports:
      - "80:80"
    environment:
      - DATABASE_URL=postgresql://postgres:62ul8dBNaA28M1y@164.52.215.211:5432/mydb
      - FIREBASE_ENABLED=true
      - GOOGLE_APPLICATION_CREDENTIALS=/app/echomed-ce18e-firebase-adminsdk-fbsvc-85b9dfcfd6.json
      - RAZORPAY_KEY_ID=rzp_test_RXeqbfnAyzjhta
      - RAZORPAY_KEY_SECRET=zoY2gbk2Pv1KM2BqJIH4NcbI
      - RAZORPAY_WEBHOOK_SECRET=RddKi?<rYm|1A'a
      - SLACK_BOT_TOKEN=xoxb-7731892011942-7731892011942-7731892011942-7731892011942
      - SLACK_WEBSITE_CHANNEL=#last-opinion-website
      - SLACK_PATIENT_QUESTIONS_CHANNEL=#lastopinion-patient-questions
      - CORS_ORIGINS=http://localhost:8081,http://localhost:8082,http://164.52.215.211:8081,http://164.52.215.211:8082,https://app.lastopinion.in,https://api.lastopinion.in
    volumes:
      - ./backend:/app
      - ./echomed-ce18e-firebase-adminsdk-fbsvc-85b9dfcfd6.json:/app/echomed-ce18e-firebase-adminsdk-fbsvc-85b9dfcfd6.json
    depends_on:
      - postgres
    restart: unless-stopped

  # PostgreSQL Database (if you want to run locally)
  postgres:
    image: postgres:15
    environment:
      - POSTGRES_DB=mydb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=62ul8dBNaA28M1y
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

